import React from 'react'
import Pool from "../components/Pool";
interface Props {
    
}

const Stake = (props: Props) => {
    return (
        <div>
            <Pool />
        </div>
    )
}

export default Stake
